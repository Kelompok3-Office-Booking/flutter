import 'package:flutter/material.dart';

class TersimpanScreen extends StatelessWidget {
  const TersimpanScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Tersimpan Screen'),
    );
  }
}
